/* Correcteur : entête Doyxgen manquant et Doxygen de la fonction
 * (dans image_content.c) non complet (explication return value)
 * -0.5pt -0.25pt
 */

#include "pictDB.h"

int lazily_resize(const int res, struct pictdb_file* file, size_t index);
